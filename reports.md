# Net Zero ArduPilot Report

Here records the bugs and solutions.

### 问题(fatal)✅️: 降落后TVS击穿。

坠机，飞控板灯灭，检查发现TVS击穿。
**解决**：关闭坠机保护，避免电机高速刹停。

---
### 问题(normal)✅️: 主机起飞时，从机不能跟从起飞

主机起飞时，从机未能收到信号。
**解决**: 没共地。插上互相供电电源线。

主机起飞时，从机收到信号也不起飞。
**解决**: 故障保护了。遇到了电压低故障保护和无遥控信号故障保护。
电压低故障保护是因为二极管压降。将保护阈值降低0.7即可。
无遥控信号故障保护，暂时在从机关闭这一保护。

---
### 问题(normal): 蓝牙难以连接， 地面站不易使用

应当研究蓝牙不互相串信号的方法。另外更换地面站。

---
### 问题(fatal): 在特定情况下可能烧毁飞控

故障现象：
1. 电调疑似有管子烧毁。
2. 飞控板外围电路和保护电路无损坏，但主控烧毁(极诡异)。

仍未定位故障原因。可能原因列举：
1. 电池和降压模块质量问题。此可能性已消除。
2. 自行并联的TVS板的质量问题。可能性不大。
3. 线路寄生电感。
4. 意外从其他引脚进入的高电压。没找到。

---
### 问题(normal): 从机会自动锁定

常规情形下，无人机解锁后如无起飞动作，将会自动锁定。
但是作为从机，解锁后，即便收到主机控制型号，也会自动锁定。

---
### 问题(normal): Slave模式下发送逻辑时序错误

**文件:** `libraries/AP_Motors/AP_MotorsMatrix.cpp:187-223`

`output_to_motors()` 中，向子机发送 `SUB_DRONE_CONTROL` 的代码（187-198行）在模式判断（201行）**之前**执行。从机在 SLAVE 模式（mode=31）下：
1. 先用**自己飞控计算的 `_actuator` 值**转换的 `control_val` 发送给它的子机
2. 然后才用 `SubDroneCache`（主机的电机值）驱动自己的电机

当前对简单2机主从关系影响较小（从机通常无子机），但语义不正确，级联场景下有bug。

---
### 问题(normal): update_router持续发送请求，无去重 (低优先级)

**文件:** `ArduCopter/Copter.cpp:269-291`

`update_router` 每100ms遍历所有有效 `backup_son` 串口并发送 `NET_ZERO_MAVLINK(test1=1)` 请求。即使子机已成功添加（`backup_son[i]` 被清为 `0xFF`），该函数仍会扫描剩余有效条目。且 `add_son_uart` 无去重逻辑，若同一从机重复发送ACK，会被多次添加到 `son[]` 数组。

---

